# G3.2 contextual drift development diagnostic / 文脈別drift開発診断 / 按情境漂移开发诊断

Status: [Done] development diagnostic, not a final ablation. Manifest: [`g3_split_contextual_drift_manifest.json`](../experiments/g3_split_contextual_drift_manifest.json). Full paired output: [`g3-split-contextual-drift-results.json`](g3-split-contextual-drift-results.json). Reproduce with `python3 -m experiments.g3_split_drift_opportunity --manifest experiments/g3_split_contextual_drift_manifest.json --output docs/g3-split-contextual-drift-results.json`.

The previous [global-reversal micro-world](G3.2-Split-Drift-Opportunity-2026-09-18.md) changed the effect in every context and triggered Primitive splits without a recovery gain. Here A1 is warm in indoor and outdoor. B changes only indoor to cold; outdoor stays warm. A2 returns indoor to warm. Split on/off share observations, probes, Replay schedule and other feature switches. Candidate specialization and merge remain disabled to isolate Primitive split.

| Measure across five development seeds | Split on | Split off |
| --- | --- | --- |
| Executed Primitive splits | 0 each | 0 each |
| Immediate A2 retention | 0.5 each | 0.5 each |
| B recovery Events | 7, 4, 6, 4, 7 | 7, 4, 6, 4, 7 |
| A2 recovery Events | 7, 7, 9, 5, 7 | 7, 7, 9, 5, 7 |

### Read-only Replay diagnostic / 読み取り専用Replay診断 / 只读Replay诊断

[Done] The B-exit diagnostic re-scores each selected **distinct learned Event once** using the current predictor. In all five seeds, the warm Primitive has 2/2 incorrect indoor evidence Events and 0/8 incorrect outdoor evidence Events. Its cumulative clean Replay score is 0.737–0.846, above the current 0.6 split-proposal threshold, with 76–91 cumulative Replay applications. The diagnostic adds no training, adoption or Replay counters; it is retrospective training-evidence inspection, **not** independent held-out accuracy. The complete per-primitive and per-context rows are in the JSON artifact.

[Done] B終了時、warm Primitiveに対応する独立証拠Eventを1回ずつ再採点すると、5 seedすべてでindoorは2/2件誤り、outdoorは0/8件誤りだった。一方、累積clean Replay scoreは0.737～0.846（76～91回の適用）で、分裂提案閾値0.6より高い。診断は状態を更新せず、学習済み証拠を遡及的に読むもので、独立held-out精度ではない。

[Done] 在B结束时，对warm原语关联的独立证据Event各重评一次：五个seed中indoor均为2/2错误，outdoor均为0/8错误；累计clean Replay分数仍为0.737至0.846（76至91次应用），高于0.6的分裂提案阈值。诊断不更新状态，只回顾训练证据，并非独立留出准确率。

[Done] 日本語: 安定したoutdoorの旧知識はA2入口でも予測できたが、split有効側でも分裂は実行されなかった。B/A2を24または48観測Eventへ伸ばす開発用追加診断でも5 seedすべて分裂0だった。seed 11のB終了時、warm Primitiveの累積clean Replay scoreは0.846（91回）で、現行の分裂提案閾値0.6を上回る。これは、局所的に変わったindoorの誤差が、安定したoutdoorや繰り返しReplayされた履歴に埋もれる**可能性**を示す。閾値を下げて帳尻を合わせず、context別の誤差と独立したEvent数を測る必要がある。

[Done] English: The stable outdoor A knowledge is still predicted at A2 entry, but split-on executes no split. Exploratory development runs with 24 or 48 observed Events per phase also execute zero splits in all five seeds. At B exit for seed 11, the warm Primitive has cumulative clean Replay score 0.846 over 91 applications, above the current 0.6 split-proposal threshold. This suggests that indoor errors may be masked by stable outdoor evidence and repeated Replay of the same history. Do not lower the threshold to force a result; measure errors by context and count independent Events separately from Replay applications.

[Done] 简体中文: 稳定的outdoor旧知识在A2入口仍可预测，但开启分裂也没有执行。将每阶段观察Event延长到24或48的开发诊断中，五个seed仍均为零次分裂。seed 11在B结束时，warm原语累计clean Replay分数为0.846（91次应用），高于当前0.6的分裂提案阈值。这提示indoor的局部错误可能被稳定outdoor证据和历史重复Replay掩盖。不能通过降低阈值制造结果；应按context统计错误，并把独立Event数与Replay应用次数分开。

The exploratory longer-phase check can be repeated with `run_split_opportunity` using the manifest above, replacing `phase_observations` with 24 or 48 and `replay_max_events` with 52 or 100 respectively. These exploratory runs did not define or inspect a final panel.

[Next] Compare a context-conditional split proposal with the current global proposal on the same development worlds, including false splits on stable contexts. Do not tune a threshold on final panels. The final G3.2 world and thresholds remain unfrozen.
