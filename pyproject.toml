[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "risa"
version = "0.1.0"
description = "Relationally Involving Self-organizing Architecture MVP scaffold"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
